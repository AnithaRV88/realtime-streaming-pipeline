# Copy to .env and adjust. Used by docker-compose for Postgres.
POSTGRES_USER=streaming
POSTGRES_PASSWORD=streaming
POSTGRES_DB=streaming

# Kafka bootstrap for apps on the host (Python producer, Spark local):
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092
